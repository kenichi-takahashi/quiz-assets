# quiz-assets

`/root/learning-quiz` が自動生成するボス画像の置き場。Google Chat のカードは公開 HTTPS URL からしか画像を読めないため、ここに置いて raw.githubusercontent.com から配信する。

- 業務データ・顧客名は入れない（クイズ本文はここに置かない）
- ファイル名に内容ハッシュを含める（Google の画像キャッシュ対策）
